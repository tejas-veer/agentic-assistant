services:
  - type: web
    name: agentic-assistant-api
    runtime: python
    pythonVersion: "3.11.7"
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.7"
      - key: DATABASE_URL
        sync: false
      - key: GEMINI_API_KEY
        sync: false
    healthCheckPath: /health

